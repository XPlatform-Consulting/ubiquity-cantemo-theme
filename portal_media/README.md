# MD theming grunt

# Setup
``` npm install ```

# Compile scss for dev
``` grunt compass ```

# To do
* add grunt watch
* compress css for dev (for grunt dev)
* include js
* compress js for dev (for grunt dev)